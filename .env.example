# Copia este archivo a `.env` y rellena tu token real.
# IMPORTANTE: .env está en .gitignore y NUNCA se sube a GitHub.

# Bearer token de la API v2 de X (Twitter)
# Necesita nivel Enterprise / Academic / DSA para acceder al endpoint search/all
X_BEARER_TOKEN=tu_token_aqui
